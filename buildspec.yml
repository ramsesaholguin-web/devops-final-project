version: 0.2

phases:
  install:
    commands:
      - echo "Instalando dependencias"
      - pip3 install -r requirements.txt || true

  build:
    commands:
      - echo "Ejecutando scripts boto3"
      - python3 boto3/list_ec2.py || true

  post_build:
    commands:
      - echo "Proceso finalizado"